{
    "name": "skylinkbingwa/admin-v2",
    "description": "Skylink Bingwa Admin V2 — offers, billboards, notifications, templates, payments, config, versioned signed sync API.",
    "type": "project",
    "license": "proprietary",
    "require": {
        "php": ">=8.2",
        "ext-pdo": "*",
        "ext-openssl": "*",
        "ext-json": "*",
        "ext-gd": "*"
    },
    "autoload": {
        "psr-4": { "App\\": "app/" }
    },
    "scripts": {
        "migrate": "php database/migrate.php",
        "seed": "php database/seed.php",
        "test": "php tests/run.php"
    },
    "config": {
        "optimize-autoloader": true
    },
    "_note": "Composer is OPTIONAL. The app ships its own PSR-4 autoloader (app/Core/Autoloader.php) so it runs on cPanel without `composer install`. This file documents PHP/extension requirements and dev scripts only."
}
